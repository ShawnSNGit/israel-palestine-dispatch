# Provenance Service

Service responsible for recording snapshot metadata, generating and storing SHA-256 fingerprints, and providing an API to query evidence lineage. This folder contains a prototyping README and recommended endpoints.
