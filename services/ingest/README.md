# Ingest Service

Small HTTP/worker service responsible for enqueuing and fetching source snapshots.

- /health
- /enqueue (admin) -> queue url to fetch and snapshot

This folder contains a minimal prototype using Flask for local development.
